---
description: 
globs: 
alwaysApply: true
---

# Your rule content

- when naming files, functions and variables, always use camel case, e.g. `testFile.ts` and `const testFunction = () => {}`
- react component files and function names start with uppercase letter, e.g. `TestFunction`
`